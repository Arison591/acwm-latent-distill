# Core deep learning
torch>=2.4.0
torchvision>=0.19.0

# Diffusion / model
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0

# VAE / video
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
decord>=0.6.0
av>=12.0.0
opencv-python>=4.9.0

# Math / metrics
numpy>=1.26.0
scipy>=1.13.0
scikit-image>=0.22.0
einops>=0.8.0

# Training utilities
wandb>=0.16.0
tqdm>=4.65.0
omegaconf>=2.3.0
huggingface-hub>=0.23.0

# Flash Attention (optional, strongly recommended for speed)
# Install separately: pip install flash-attn --no-build-isolation
