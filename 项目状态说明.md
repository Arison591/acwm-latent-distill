# ACWM Latent Distillation 项目状态说明

## 1. Idea 现在的判断

这个项目目前最值得保留的核心问题不是“action latent 一定比 raw action 好”，而是：

> student 在被压缩或蒸馏之后，能不能继续保留 teacher 对不同动作的响应差异。

换句话说，项目真正要验证的是 **action response / controllability preservation**。这比单纯说“做 latent action”更有研究价值，因为 latent action、world model distillation、specialist-to-generalist distillation 都已经有相邻工作；更锋利的切口应该是：**在 ACWM-Phys 这种 action-conditioned world model benchmark 上，用 response KD 验证模型是否真的听 action**。

当前判断：

- 值得做成一个小而清楚的研究项目。
- 不建议一开始做大模型或复杂 VQ/RVQ tokenizer。
- 第一阶段应该先用 continuous action encoder、small/large teachers、response KD 和 action ablation 跑通闭环。
- 如果 specialist teacher 在自己的 action bucket 内都不能超过 generalist，那么后续蒸馏就应该暂停或重设 split。

最重要的风险是：模型可能视觉指标不错，但其实几乎不响应 action。因此后续评估必须包含 `zero / reverse / scale_0_5` 这类 action ablation，而不能只看 MSE、PSNR、SSIM。

## 2. 这次已经做了什么

项目已经基于 `xavihart/ACWM-Phys-dev` 搭在：

```bash
/root/acwm-latent-distill
```

当前工作分支：

```bash
main
```

这次完成的是一个“研究可跑骨架”，不是完整实验结果。具体包括：

### 2.1 Action latent 模块

新增目录：

```bash
acwm/action_latent/
```

里面包含：

- `IdentityActionEncoder`
- `MLPActionEncoder`
- `ConvActionEncoder`
- action magnitude bucket 计算
- small / large action bucket 分配
- counterfactual action 生成：`zero`、`reverse`、`scale_0_5`，并预留 `scale_2`、`shuffle`

现在 `DiffusionForcing_WM` 已支持可选配置：

```yaml
model_config:
  action_encoder:
    type: "mlp"
    latent_dim: 64
```

也就是说 raw action 会先通过 action encoder，再进入 DiT 的 action conditioning。

### 2.2 Distillation 模块

新增目录：

```bash
acwm/distill/
```

里面包含：

- `prediction_loss`
- `kd_loss`
- `response_kd_loss`
- teacher checkpoint state dict 加载工具
- bucket teacher ensemble 的基础封装

response KD 当前是在 flow-matching velocity / feature 空间里做，不直接在像素空间硬对齐。这个选择更稳，因为像素级 counterfactual 对齐容易把模型推向模糊平均。

### 2.3 训练路径接入

修改了：

```bash
acwm/dynamics/diffusion_forcing_wm.py
acwm/trainer/train_dynamics.py
```

主要变化：

- `DiffusionForcing_WM` 增加 `encode_action`
- 增加 `training_outputs(...)`，暴露 `v_pred`、`v_target`、`z_t`、`t_values`、`eps`
- 训练时可选择启用 distillation
- 支持 small / large teacher checkpoint
- 支持 action bucket 过滤，用于训练 specialist teachers
- 修正了原训练脚本加载 `configs/model/dit_s.yaml` 的路径问题

当前 student loss 结构是：

```text
L = L_pred + lambda_kd * L_kd + mu_resp * L_resp
```

默认：

```yaml
lambda_kd: 0.25
mu_resp: 0.5
counterfactuals: ["zero", "reverse", "scale_0_5"]
```

### 2.4 配置文件

新增：

```bash
configs/alrd/push_cube_latent.yaml
configs/alrd/push_cube_teacher_small.yaml
configs/alrd/push_cube_teacher_large.yaml
configs/alrd/push_cube_student_resp_kd.yaml
```

这些配置默认都是小步数、小数据量，目的是先做 smoke / feasibility，不是正式论文实验。

### 2.5 脚本

新增：

```bash
scripts/alrd/inspect_actions.py
scripts/alrd/make_action_buckets.py
scripts/alrd/eval_action_ablation.py
scripts/alrd/run_push_cube_smoke.sh
```

用途分别是：

- 查看 action 维度、长度、magnitude 分布
- 生成 action bucket 缓存
- 对 `a / zero / reverse / 0.5a` 做 action ablation
- 跑无数据 smoke test 和 YAML 加载检查

### 2.6 文档和测试

新增：

```bash
README_ALRD.md
docs/experiment_protocol.md
docs/go_no_go.md
tests/alrd/test_smoke.py
```

`README_ALRD.md` 是英文快速入口。  
`docs/experiment_protocol.md` 写了实验顺序。  
`docs/go_no_go.md` 写了继续做和止损的判断标准。  
`tests/alrd/test_smoke.py` 验证 action encoder、bucket、counterfactual 和 distillation loss 的基本行为。

## 3. 已经验证过什么

原始 scaffold 阶段已跑过：

```bash
python -m compileall acwm scripts/alrd tests/alrd
python tests/alrd/test_smoke.py
bash scripts/alrd/run_push_cube_smoke.sh
```

结果：

- Python 编译检查通过
- 无数据 tensor smoke test 通过
- ALRD YAML 配置加载通过
- 当时因为没有设置 `WAN_VAE_PATH`，真实训练 forward smoke 被脚本按预期跳过

### 3.1 2026-07-07 本地环境准备更新

现在 `/root/acwm-latent-distill` 本机已经补齐了真实 smoke 所需环境：

- 安装了训练依赖，包括 `diffusers`、`transformers`、`decord`、`imageio`、`scikit-image`、`omegaconf`、`moviepy`
- 下载了 Push Cube 当前 ALRD smoke 需要的 ACWM-Phys 子集到 `data/`
- 下载了 `Wan2.1_VAE.pth` 到 `checkpoints/Wan2.1_VAE.pth`
- 新增 `.env.example`，本地 `.env.local` 已设置 `ACWM_DATA_ROOT`、`WAN_VAE_PATH`、`WANDB_MODE=offline`
- wandb 当前走 offline，本地 run 在 `wandb/`，后续可用 `wandb sync ...` 上传

这次发现并修了几个会导致训练跑不稳的问题：

- `BaseRoboticsDataset(max_trajs=...)` 采样后索引错位，会读到前 N 个 episode；现在保留原始 trajectory id
- Push Cube action magnitude 恒为 1.0，原 small/large magnitude split 会退化成 large 空集；现在自动 fallback 到 `signed_action_0`
- student config 的 teacher checkpoint 路径缺少 `_240x240` 后缀；现在已修正
- checkpoint 的 `latest.pt` 现在用硬链接指向 numbered checkpoint，避免每个 run 重复占 3GB

已经真实跑通：

```bash
source .env.local
python train.py --config configs/alrd/push_cube_latent.yaml
python train.py --config configs/alrd/push_cube_teacher_small.yaml
python train.py --config configs/alrd/push_cube_teacher_large.yaml
python train.py --config configs/alrd/push_cube_student_resp_kd.yaml
```

已生成 checkpoint：

```bash
checkpoints/alrd_push_cube_latent_smoke_240x240/latest.pt
checkpoints/alrd_push_cube_teacher_small_240x240/latest.pt
checkpoints/alrd_push_cube_teacher_large_240x240/latest.pt
checkpoints/alrd_push_cube_student_resp_kd_240x240/latest.pt
```

## 4. 接下来怎么入手

第一步，准备数据和 VAE：

```bash
export ACWM_DATA_ROOT=/path/to/ACWM-Phys
export WAN_VAE_PATH=/path/to/Wan2.1_VAE.pth
```

第二步，先看 Push Cube 的 action 分布：

```bash
cd /root/acwm-latent-distill
python scripts/alrd/inspect_actions.py --env push_cube --split ind_train
python scripts/alrd/make_action_buckets.py --env push_cube --split ind_train
```

第三步，跑 latent-action baseline：

```bash
WANDB_MODE=disabled python train.py --config configs/alrd/push_cube_latent.yaml
```

第四步，分别训练 small / large teachers：

```bash
WANDB_MODE=disabled python train.py --config configs/alrd/push_cube_teacher_small.yaml
WANDB_MODE=disabled python train.py --config configs/alrd/push_cube_teacher_large.yaml
```

第五步，确认 teachers 在各自 bucket 上确实优于 generalist。这个检查非常关键，如果 teacher 本身不强，不要继续做 distillation。

第六步，训练 response-KD student：

```bash
WANDB_MODE=disabled python train.py --config configs/alrd/push_cube_student_resp_kd.yaml
```

第七步，跑 action ablation：

```bash
python scripts/alrd/eval_action_ablation.py \
  --env push_cube \
  --cfg configs/alrd/push_cube_student_resp_kd.yaml \
  --ckpt checkpoints/alrd_push_cube_student_resp_kd_240x240/latest.pt \
  --steps 20 \
  --save_videos
```

## 5. 成败标准

继续推进的信号：

- latent baseline 不弱于 raw-action baseline
- small / large specialist 在自己的 bucket 内超过 generalist
- response KD 比普通 KD 更能区分 `a`、`zero`、`reverse`、`0.5a`
- action ablation 的差异变大，但真实 action rollout 的视觉指标没有明显崩

应该暂停或重设方案的信号：

- specialist teacher 不如 generalist
- `pred(a)`、`pred(0)`、`pred(-a)` 仍然几乎一样
- response KD 只是制造更大的预测差异，但真实 rollout 更差
- action latent 只是在增加参数量，没有带来 controllability 改善

## 6. 当前结论

这个 idea 现在已经从“概念判断”推进到了“可实验工程骨架”。下一步不是继续堆模块，而是跑最小实验，回答一个非常具体的问题：

> response KD 是否真的比普通 prediction KD 更能保留 action-conditioned world model 的动作响应？

如果 Push Cube / Reacher 上能看到这个信号，再考虑更复杂的 split、VQ/RVQ action tokenizer、Robot Arm / Cloth Move 等高维或复杂动力学环境。

## 7. 2026-07-11 验证更新：方向 specialist response KD 有可复现的 work 信号

这次没有继续堆模块，而是先修正了验证本身的两个问题：

- action ablation 现在在 `a / zero / reverse / 0.5a` 间复用**同一份初始扩散噪声**。此前每个条件独立采样，模型即使忽略 action 也会因为噪声不同而看起来“有响应”。
- 数据集现在会跳过缺失视频，并在真正读取不到视频时失败，而不是悄悄补全零帧。发现 OOD Push Cube 本地缺 34/50 个视频后，已把所需文件下载到独立数据盘 `/root/autodl-tmp/acwm-phys/`，再以符号链接接入；系统盘没有因此增长。训练 split 也从 584/1500 补到 1129/1500 个可用视频，剩余 371 个因 HF 未认证请求反复 TLS 断流未完成；扩大训练前必须继续补齐并重估 bucket。

### 7.1 先验证 latent interface

在相同的 100 step、256 trajectories、DiT-S 控制实验中：

| 模型 | ID true-action MSE | ID PSNR |
| --- | ---: | ---: |
| raw action | 0.01742 | 17.59 |
| continuous MLP latent action | 0.01594 | 17.97 |

因此 continuous action latent 作为 interface 有小但明确的正向视觉信号（MSE 约降低 8.5%）；这不是纯粹增加参数后崩坏的结果。

### 7.2 原始 ensemble 为什么不 work

Push Cube 的每步 action L2 magnitude 恒为 1，所以所谓 `small / large magnitude` split 实际退化成第一维 action 的正负方向 split。现有 `small` teacher 有明显 response，但 `large` teacher 在配对噪声 ablation 下几乎 action-blind。把这两个 teacher 都用于 KD，或仅把 `mu_resp` 提高到 2.0，都只能得到约 `5.6e-6` 的 `MSE(pred(a), pred(-a))`，没有可靠的 controllability 改善。

结论：**当前“所有 bucket ensemble + velocity response KD”版本不 work**，根因是未通过 oracle gate 的 teacher 被当作监督信号，而不是 response 权重太小。

### 7.3 可工作的改进版

保留唯一已通过 oracle gate 的 direction specialist，并且只在该 direction bucket 内训练 response-KD student。当前可用训练 split 下的 direction 阈值是 `signed_action_0 = -0.018154`。评估固定输入、固定初始噪声、10 denoising steps、seed 123、8 batches：

| split / direction bucket | 模型 | true-action MSE | reverse sensitivity `MSE(pred(a), pred(-a))` |
| --- | --- | ---: | ---: |
| ID / small | latent-only baseline | 0.016894 | 1.975e-7 |
| ID / small | single-specialist response KD | 0.016965 | 2.690e-5 |
| OOD / small | latent-only baseline | 0.013030 | 2.068e-7 |
| OOD / small | single-specialist response KD | 0.012920 | 3.159e-5 |

因此，改进版的 `reverse` action response 在 ID 提升约 **136×**、OOD 提升约 **153×**；视觉 rollout 没有明显牺牲，OOD 反而略好。这是当前 idea 的 work 信号，但它只支持以下更窄的表述：

> 在经过 oracle 验证的方向 specialist 内，response KD 可以使 compact student 保留更强的 counterfactual action response。

### 7.4 当前局限与下一步

- 目前只有 Push Cube、一个 direction bucket、一个随机种子和 100 step feasibility run，不能声称通用方法。
- 当前本机 `ind_train` 仍未完全补齐（1129/1500 可用）。ID/OOD eval split 已可用，但任何新训练、teacher 对比或正式表格都要先补齐 train split 并重算 bucket。
- `-a` 与 `0.5a` 没有对应的真实 counterfactual GT，因此现在证明的是 response preservation，不是反事实物理正确性。
- student 的 response 仍小于 teacher；需要多 seed 和更长训练检验是否会继续缩小差距。
- Push Cube 不适合 magnitude specialist。下一步应先在 Reacher 或动作 magnitude 有真实变化的环境重复 oracle gate；只有两侧 specialist 都通过，才恢复多-specialist ensemble。

详细可复现实验记录见 `docs/alrd_validation_2026-07-11.md`。正式训练请优先使用 `--no_checkpoints` 做筛选，并将中间权重导出到 `/dev/shm` 或数据盘；保留的 checkpoint 数量必须受限，避免当前系统盘被 3GB/份 optimizer checkpoint 写满。
